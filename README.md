# IntentList

Evidence-first, human-reviewed pipeline that finds public wanted-to-buy requests for high-value collectibles,
verifies emails, de-duplicates across every batch, and delivers Excel batches.

This repository is being built as a stack of 10 feature PRs. The full README arrives with the last one.
See `CLAUDE.md` for architecture and the invariants every PR is reviewed against.
